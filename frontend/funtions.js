const birinchijavob = getSelection("birinchijavob").value

let test2listjavob = [birinchijavob]

function checking(test2listjavob) {

    if (test2listjavob.birinchijavob= text)
        count = + count 
    
}
